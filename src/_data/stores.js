// Loads the editable source JSON so pagination can use `stores.stores`.
// Edit stores.source.json to add / update stores — this file rarely changes.
module.exports = require("./stores.source.json");
